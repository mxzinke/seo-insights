# dataforseo — opt-in DataForSEO integration toolkit.
# This package is NOT imported by the default pipeline (run.sh / build_report_data.py).
# It only runs when a user explicitly invokes a cli_*.py script AND provides credentials.
